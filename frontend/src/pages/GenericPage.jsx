import React from 'react';export default function GenericPage({title,eyebrow,text}){return <div className="page"><div className="pageTop"><div><div className="eyebrow">{eyebrow}</div><h1 className="pageTitle">{title}</h1><p className="pageDesc">{text}</p></div></div><div className="genericGrid"><div className="panel placeholder"><div className="placeholderIcon">✦</div><h2>{title} workspace</h2><p>The visual foundation is ready. Real data and business logic will be connected in the next implementation phase.</p></div><div className="panel placeholder"><div className="placeholderIcon">◌</div><h2>Ready for data</h2><p>Reusable tables, filters, cards and states will plug into the backend without changing the shell.</p></div></div></div>}
