# Database URL (using psycopg v3 driver)
DATABASE_URL=postgresql+psycopg://postgres:password@localhost:5432/storepilot

# Application Environment (development, production)
APP_ENV=development

# Debug Mode
DEBUG=true

# CORS Origins (comma-separated list, or JSON list)
CORS_ORIGINS=["http://localhost:5173"]
